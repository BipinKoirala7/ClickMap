import { registry } from "@/openapi/registry.ts";

registry.registerPath({
  path: "/",
  method: "get",
  responses: {},
});

registry.registerPath({
  path: "/",
  method: "put",
  responses: {},
});
